version: 2

models:
  - name: materialization_incremental
    description: demo to show incremental model
    columns:
      - name: customer_key
        tests:
          - unique
          - not_null

  - name: use_variables
    description: demo to show variables
    columns:
      - name: order_item_key
        tests:
          - unique
          - not_null 

  - name: get_area_of_circle
    description: demo to show udfs

  - name: my_first_model
    description: demo to show model
    columns:
      - name: id
        tests:
          - unique
          - not_null
        
  - name: my_second_model
    description: demo to show model
    columns:
      - name: id
        tests:
          - unique
          - not_null